# tenant

The per-tenant OpenClaw runtime pod. This is a slim Node container — OpenClaw is **not** baked into the image. Instead, `entrypoint.sh` installs OpenClaw into the tenant's persistent GCS bucket on first boot and reuses the installation on subsequent starts. This lets tenants update independently without requiring a new Docker image.

## How it works

```
Pod starts
  └── entrypoint.sh
        1. Mount points exist: /data/openclaw (GCS), /data/secrets (emptyDir), /shared-skills, /config
        2. Check $RUNTIME_DIR/node_modules/.bin/openclaw
           └── not found → npm install openclaw@$OPENCLAW_VERSION into GCS bucket
           └── found     → reuse existing install (fast restart)
        3. Copy /config/openclaw.json to state dir if not already present
        4. Symlink /shared-skills/{org,team} into state dir
        5. exec openclaw gateway run --bind lan --port 18789
```

## Storage layout (inside the pod)

| Mount path | Volume type | Contents |
|------------|-------------|----------|
| `/data/openclaw` | GCS Fuse CSI (or PVC fallback) | OpenClaw install, sessions, agent state — **persists across restarts** |
| `/data/secrets` | emptyDir (Memory) | Personal API keys encrypted with the per-tenant key — **ephemeral** |
| `/shared-skills` | PVC (ReadWriteMany) | Org/team skills symlinked into the OpenClaw skills directory |
| `/config` | ConfigMap | `openclaw.json` generated by the operator from base config + spec overrides |
| `/etc/openclaw/encryption-key` | K8s Secret | 256-bit AES key for personal secret encryption |

## Environment variables

| Variable | Description |
|----------|-------------|
| `OPENCLAW_STATE_DIR` | Path to the persistent state directory (`/data/openclaw`) |
| `OPENCLAW_SECRETS_DIR` | Path to the ephemeral secrets directory (`/data/secrets`) |
| `OPENCLAW_ENCRYPTION_KEY_PATH` | Path to the per-tenant encryption key file |
| `OPENCLAW_TENANT_NAME` | Tenant identifier (set by the operator) |
| `OPENCLAW_VERSION` | npm version to install (e.g. `latest`, `2026.3.15`). Defaults to `latest`. |

## Self-updating OpenClaw

Since OpenClaw is installed into the GCS bucket (not the Docker image), tenants can update independently:

1. Set `spec.openclawVersion: "2026.3.16"` on the `Tenant` CR
2. The operator sets `OPENCLAW_VERSION=2026.3.16` in the Deployment env
3. On next pod start, `entrypoint.sh` detects a version mismatch and reinstalls

This means the Docker image only needs to be rebuilt when the base OS or Node runtime changes, not when OpenClaw itself releases a new version.

## Files

```
deploy/
├── Dockerfile          # Slim Node 22 runtime, no OpenClaw
└── entrypoint.sh       # First-boot init + OpenClaw install + gateway start

config/
└── base-openclaw-config.json   # Base config template (merged with spec.configOverrides by operator)
```

## Docker

Built from `deploy/Dockerfile` using the repo root as build context:

```bash
docker build -f apps/tenant/deploy/Dockerfile -t ghcr.io/opencrane/tenant:latest .
```
